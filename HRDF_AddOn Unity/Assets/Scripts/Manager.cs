using UnityEngine;
using System.Collections;

public class Manager : MonoBehaviour {

    // Use this for initialization
    void Start () {
        // DB db = new DB(); // MonoBehaviour 에서 new 안됨 어떤식으로 해야할지는 공부할 필요 잇음
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
